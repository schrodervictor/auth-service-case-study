apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "marta.fullname" . }}-config
  labels:
    {{- include "marta.labels" . | nindent 4 }}
data:
  config.json: |
    {{- .Values.config | toJson | nindent 4 }}
