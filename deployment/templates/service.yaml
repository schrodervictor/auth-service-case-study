apiVersion: v1
kind: Service
metadata:
  name: {{ include "marta.fullname" . }}
  labels:
    {{- include "marta.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "marta.selectorLabels" . | nindent 4 }}
